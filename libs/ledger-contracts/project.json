{
  "name": "ledger-contracts",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ledger-contracts/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:ledger"],
  "// targets": "to see all targets run: nx show project ledger-contracts --web",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/ledger-contracts",
        "main": "libs/ledger-contracts/src/index.ts",
        "tsConfig": "libs/ledger-contracts/tsconfig.lib.json",
        "assets": ["libs/ledger-contracts/*.md"],
        "format": ["cjs"]
      }
    }
  }
}
