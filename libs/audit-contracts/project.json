{
  "name": "audit-contracts",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/audit-contracts/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:audit"],
  "// targets": "to see all targets run: nx show project audit-contracts --web",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/audit-contracts",
        "main": "libs/audit-contracts/src/index.ts",
        "tsConfig": "libs/audit-contracts/tsconfig.lib.json",
        "assets": ["libs/audit-contracts/*.md"],
        "format": ["cjs"]
      }
    }
  }
}
